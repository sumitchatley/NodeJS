install node js
create a folder for the project
Open VS Code.. press ctrl+~(open terminal)
run npm init  (it will generate package.json for you)
run npm install (it will install the dependencies)
copy paste index.js and chat.html in your project.
check the port in index.js.
run - node index.js and the chat app will be available at  http://localhost:<port number>
